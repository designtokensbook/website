export type Phase = {
    id: string;
    theme: string;
    phases: string[];
};
  
export type Tool = {
    title: string;
    description: string;
    imageSrc: string;
};
  
export type Rex = {
    name: string;
    userImage:string;
    entrepriseImage: string;
    description: string;
};
  