---
import { Badge } from "@/components/ui/badge";
import { Image } from "astro:assets";
import type { CollectionEntry } from "astro:content";

type Props = CollectionEntry<"links">;

const post = Astro.props;
---

<article class="group space-y-4">
  <a
    href={`/links/${post.slug}/`}
    class="flex h-52 group-hover:-translate-y-2 group-hover:shadow-xl transition duration-300"
  >
    <Image
      class="w-full h-full object-cover rounded-xl overflow-hidden"
      width={400}
      height={200}
      src={post.data.coverImage}
      alt="descriptive text"
      transition:name={"img-" + post.slug}
    />
  </a>

  <div class="flex items-center justify-between">
    <a href={`/links/category/${post.data.category}`}>
      <Badge radius="md" transition:name={"category-" + post.slug}>
        {post.data.category}
      </Badge>
    </a>
  </div>

  <div>
    <a href={`/links/${post.slug}/`} class="group-hover:underline">
      <h2
        class="font-heading text-xl md:text-2xl leading-snug line-clamp-3"
        transition:name={"title-" + post.slug}
      >
        {post.data.title}
      </h2>
    </a>
  </div>
</article>
